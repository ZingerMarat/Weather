import React from "react";

//create component
const Info = (props) => {
  return(
    <div>
      <h2>Weather App</h2>
      <p>Check weather in your city</p>
    </div>  
  );
}

export default Info;
